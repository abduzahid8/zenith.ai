diff --git a/node_modules/react-native-chessboard/lib/commonjs/components/piece/index.js b/node_modules/react-native-chessboard/lib/commonjs/components/piece/index.js
index 2ebf953..8e122df 100644
--- a/node_modules/react-native-chessboard/lib/commonjs/components/piece/index.js
+++ b/node_modules/react-native-chessboard/lib/commonjs/components/piece/index.js
@@ -76,13 +76,27 @@ const Piece = /*#__PURE__*/_react.default.memo( /*#__PURE__*/_react.default.forw
     } = _ref2;
     onMove(move.from, move.to);
   }, [onMove]);
-  const moveTo = (0, _react.useCallback)((from, to) => {
+  const moveTo = (0, _react.useCallback)((from, to, force = false, isSilent = false) => {
     return new Promise(resolve => {
       const move = validateMove(from, to);
+      const targetSquare = move ? move.to : (force ? to : from);
       const {
         x,
         y
-      } = toTranslation(move ? move.to : from);
+      } = toTranslation(targetSquare);
+      const isCastling = move && (move.flags.includes('k') || move.flags.includes('q'));
+      if (isCastling) {
+        let rookFrom = move.color === 'w' 
+          ? (move.flags.includes('k') ? 'h1' : 'a1') 
+          : (move.flags.includes('k') ? 'h8' : 'a8');
+        let rookTo = move.color === 'w' 
+          ? (move.flags.includes('k') ? 'f1' : 'd1') 
+          : (move.flags.includes('k') ? 'f8' : 'd8');
+        const rookRef = refs === null || refs === void 0 ? void 0 : refs.current?.[rookFrom]?.current;
+        if (rookRef) {
+          rookRef.moveTo(rookTo, true, true);
+        }
+      }
       translateX.value = (0, _reactNativeReanimated.withTiming)(x, {
         duration: moveDuration
       }, () => {
@@ -95,7 +109,7 @@ const Piece = /*#__PURE__*/_react.default.memo( /*#__PURE__*/_react.default.forw
         offsetY.value = translateY.value;
         isGestureActive.value = false;
 
-        if (move) {
+        if (move && !isSilent) {
           (0, _reactNativeReanimated.runOnJS)(wrappedOnMoveForJSThread)({
             move
           }); // Ideally I must call the resolve method
@@ -110,7 +124,7 @@ const Piece = /*#__PURE__*/_react.default.memo( /*#__PURE__*/_react.default.forw
         }
       });
     });
-  }, [isGestureActive, moveDuration, offsetX, offsetY, toTranslation, translateX, translateY, validateMove, wrappedOnMoveForJSThread]);
+  }, [isGestureActive, moveDuration, offsetX, offsetY, toTranslation, translateX, translateY, validateMove, wrappedOnMoveForJSThread, refs]);
   const movePiece = (0, _react.useCallback)(to => {
     const from = toPosition({
       x: offsetX.value,
@@ -120,8 +134,8 @@ const Piece = /*#__PURE__*/_react.default.memo( /*#__PURE__*/_react.default.forw
   }, [moveTo, offsetX.value, offsetY.value, toPosition]);
   (0, _react.useImperativeHandle)(ref, () => {
     return {
-      moveTo: to => {
-        return moveTo(square, to);
+      moveTo: (to, force = false, isSilent = false) => {
+        return moveTo(square, to, force, isSilent);
       },
       enable: active => {
         pieceEnabled.value = active;
diff --git a/node_modules/react-native-chessboard/lib/commonjs/context/board-refs-context/index.js b/node_modules/react-native-chessboard/lib/commonjs/context/board-refs-context/index.js
index fdc9379..f55a3a1 100644
--- a/node_modules/react-native-chessboard/lib/commonjs/context/board-refs-context/index.js
+++ b/node_modules/react-native-chessboard/lib/commonjs/context/board-refs-context/index.js
@@ -60,7 +60,7 @@ const BoardRefsContextProviderComponent = /*#__PURE__*/_react.default.forwardRef
         from,
         to
       } = _ref2;
-      return pieceRefs === null || pieceRefs === void 0 ? void 0 : (_pieceRefs$current = pieceRefs.current) === null || _pieceRefs$current === void 0 ? void 0 : (_pieceRefs$current$fr = _pieceRefs$current[from].current) === null || _pieceRefs$current$fr === void 0 ? void 0 : (_pieceRefs$current$fr2 = _pieceRefs$current$fr.moveTo) === null || _pieceRefs$current$fr2 === void 0 ? void 0 : _pieceRefs$current$fr2.call(_pieceRefs$current$fr, to);
+      return pieceRefs === null || pieceRefs === void 0 ? void 0 : (_pieceRefs$current = pieceRefs.current) === null || _pieceRefs$current === void 0 ? void 0 : (_pieceRefs$current$fr = _pieceRefs$current[from].current) === null || _pieceRefs$current$fr === void 0 ? void 0 : (_pieceRefs$current$fr2 = _pieceRefs$current$fr.moveTo) === null || _pieceRefs$current$fr2 === void 0 ? void 0 : _pieceRefs$current$fr2.call(_pieceRefs$current$fr, to, true);
     },
     highlight: _ref3 => {
       var _squareRefs$current;
diff --git a/node_modules/react-native-chessboard/lib/module/components/piece/index.js b/node_modules/react-native-chessboard/lib/module/components/piece/index.js
index 9d3af78..5056114 100644
--- a/node_modules/react-native-chessboard/lib/module/components/piece/index.js
+++ b/node_modules/react-native-chessboard/lib/module/components/piece/index.js
@@ -55,13 +55,27 @@ const Piece = /*#__PURE__*/React.memo( /*#__PURE__*/React.forwardRef((_ref, ref)
     } = _ref2;
     onMove(move.from, move.to);
   }, [onMove]);
-  const moveTo = useCallback((from, to) => {
+  const moveTo = useCallback((from, to, force = false, isSilent = false) => {
     return new Promise(resolve => {
       const move = validateMove(from, to);
+      const targetSquare = move ? move.to : (force ? to : from);
       const {
         x,
         y
-      } = toTranslation(move ? move.to : from);
+      } = toTranslation(targetSquare);
+      const isCastling = move && (move.flags.includes('k') || move.flags.includes('q'));
+      if (isCastling) {
+        let rookFrom = move.color === 'w' 
+          ? (move.flags.includes('k') ? 'h1' : 'a1') 
+          : (move.flags.includes('k') ? 'h8' : 'a8');
+        let rookTo = move.color === 'w' 
+          ? (move.flags.includes('k') ? 'f1' : 'd1') 
+          : (move.flags.includes('k') ? 'f8' : 'd8');
+        const rookRef = refs === null || refs === void 0 ? void 0 : refs.current?.[rookFrom]?.current;
+        if (rookRef) {
+          rookRef.moveTo(rookTo, true, true);
+        }
+      }
       translateX.value = withTiming(x, {
         duration: moveDuration
       }, () => {
@@ -74,7 +88,7 @@ const Piece = /*#__PURE__*/React.memo( /*#__PURE__*/React.forwardRef((_ref, ref)
         offsetY.value = translateY.value;
         isGestureActive.value = false;
 
-        if (move) {
+        if (move && !isSilent) {
           runOnJS(wrappedOnMoveForJSThread)({
             move
           }); // Ideally I must call the resolve method
@@ -89,7 +103,7 @@ const Piece = /*#__PURE__*/React.memo( /*#__PURE__*/React.forwardRef((_ref, ref)
         }
       });
     });
-  }, [isGestureActive, moveDuration, offsetX, offsetY, toTranslation, translateX, translateY, validateMove, wrappedOnMoveForJSThread]);
+  }, [isGestureActive, moveDuration, offsetX, offsetY, toTranslation, translateX, translateY, validateMove, wrappedOnMoveForJSThread, refs]);
   const movePiece = useCallback(to => {
     const from = toPosition({
       x: offsetX.value,
@@ -99,8 +113,8 @@ const Piece = /*#__PURE__*/React.memo( /*#__PURE__*/React.forwardRef((_ref, ref)
   }, [moveTo, offsetX.value, offsetY.value, toPosition]);
   useImperativeHandle(ref, () => {
     return {
-      moveTo: to => {
-        return moveTo(square, to);
+      moveTo: (to, force = false, isSilent = false) => {
+        return moveTo(square, to, force, isSilent);
       },
       enable: active => {
         pieceEnabled.value = active;
diff --git a/node_modules/react-native-chessboard/lib/module/context/board-refs-context/index.js b/node_modules/react-native-chessboard/lib/module/context/board-refs-context/index.js
index c92032c..f54cf3e 100644
--- a/node_modules/react-native-chessboard/lib/module/context/board-refs-context/index.js
+++ b/node_modules/react-native-chessboard/lib/module/context/board-refs-context/index.js
@@ -42,7 +42,7 @@ const BoardRefsContextProviderComponent = /*#__PURE__*/React.forwardRef((_ref, r
         from,
         to
       } = _ref2;
-      return pieceRefs === null || pieceRefs === void 0 ? void 0 : (_pieceRefs$current = pieceRefs.current) === null || _pieceRefs$current === void 0 ? void 0 : (_pieceRefs$current$fr = _pieceRefs$current[from].current) === null || _pieceRefs$current$fr === void 0 ? void 0 : (_pieceRefs$current$fr2 = _pieceRefs$current$fr.moveTo) === null || _pieceRefs$current$fr2 === void 0 ? void 0 : _pieceRefs$current$fr2.call(_pieceRefs$current$fr, to);
+      return pieceRefs === null || pieceRefs === void 0 ? void 0 : (_pieceRefs$current = pieceRefs.current) === null || _pieceRefs$current === void 0 ? void 0 : (_pieceRefs$current$fr = _pieceRefs$current[from].current) === null || _pieceRefs$current$fr === void 0 ? void 0 : (_pieceRefs$current$fr2 = _pieceRefs$current$fr.moveTo) === null || _pieceRefs$current$fr2 === void 0 ? void 0 : _pieceRefs$current$fr2.call(_pieceRefs$current$fr, to, true);
     },
     highlight: _ref3 => {
       var _squareRefs$current;
diff --git a/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts b/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
index 42e9bea..12e69db 100644
--- a/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
+++ b/node_modules/react-native-chessboard/lib/typescript/context/props-context/index.d.ts
@@ -58,8 +58,9 @@ declare type ChessboardProps = {
      * Useful if you want to customise the default durations used in the chessboard (in milliseconds).
      */
     durations?: ChessboardDurationsType;
+    boardOrientation?: 'white' | 'black';
 };
-declare type ChessboardContextType = ChessboardProps & Required<Pick<ChessboardProps, 'gestureEnabled' | 'withLetters' | 'withNumbers' | 'boardSize'>> & {
+declare type ChessboardContextType = ChessboardProps & Required<Pick<ChessboardProps, 'gestureEnabled' | 'withLetters' | 'withNumbers' | 'boardSize' | 'boardOrientation'>> & {
     pieceSize: number;
 } & {
     colors: Required<ChessboardColorsType>;
diff --git a/node_modules/react-native-chessboard/src/components/piece/index.tsx b/node_modules/react-native-chessboard/src/components/piece/index.tsx
index bbbd539..58d9685 100644
--- a/node_modules/react-native-chessboard/src/components/piece/index.tsx
+++ b/node_modules/react-native-chessboard/src/components/piece/index.tsx
@@ -43,6 +43,7 @@ const Piece = React.memo(
       const {
         durations: { move: moveDuration },
         gestureEnabled: gestureEnabledFromChessboardProps,
+        boardOrientation,
       } = useChessboardProps();
 
       const gestureEnabled = useDerivedValue(
@@ -57,8 +58,9 @@ const Piece = React.memo(
       const offsetY = useSharedValue(0);
       const scale = useSharedValue(1);
 
-      const translateX = useSharedValue(startPosition.x * size);
-      const translateY = useSharedValue(startPosition.y * size);
+      const isFlipped = boardOrientation === 'black';
+      const translateX = useSharedValue((isFlipped ? 7 - startPosition.x : startPosition.x) * size);
+      const translateY = useSharedValue((isFlipped ? 7 - startPosition.y : startPosition.y) * size);
 
       const validateMove = useCallback(
         (from: Square, to: Square) => {
@@ -77,10 +79,37 @@ const Piece = React.memo(
       );
 
       const moveTo = useCallback(
-        (from: Square, to: Square) => {
+        (from: Square, to: Square, force: boolean = false, isSilent: boolean = false) => {
           return new Promise<Move | undefined>((resolve) => {
             const move = validateMove(from, to);
-            const { x, y } = toTranslation(move ? move.to : from);
+            if (move && (move.flags.includes('c') || move.flags.includes('e'))) {
+              let capturedSquare = move.to;
+              if (move.flags.includes('e')) {
+                const file = move.to[0];
+                const rank = move.color === 'w' ? '5' : '4';
+                capturedSquare = `${file}${rank}` as Square;
+              }
+              const capturedPieceRef = refs?.current?.[capturedSquare]?.current;
+              if (capturedPieceRef) {
+                capturedPieceRef.enable(false);
+              }
+            }
+            const targetSquare = move ? move.to : (force ? to : from);
+            const { x, y } = toTranslation(targetSquare);
+            const isCastling = move && (move.flags.includes('k') || move.flags.includes('q'));
+            if (isCastling) {
+              let rookFrom: Square = move.color === 'w' 
+                ? (move.flags.includes('k') ? 'h1' : 'a1') 
+                : (move.flags.includes('k') ? 'h8' : 'a8');
+              let rookTo: Square = move.color === 'w' 
+                ? (move.flags.includes('k') ? 'f1' : 'd1') 
+                : (move.flags.includes('k') ? 'f8' : 'd8');
+              const rookRef = refs?.current?.[rookFrom]?.current;
+              if (rookRef) {
+                rookRef.moveTo(rookTo, true, true);
+              }
+            }
+
             translateX.value = withTiming(x, { duration: moveDuration }, () => {
               offsetX.value = translateX.value;
             });
@@ -91,7 +120,7 @@ const Piece = React.memo(
                 if (!isFinished) return;
                 offsetY.value = translateY.value;
                 isGestureActive.value = false;
-                if (move) {
+                if (move && !isSilent) {
                   runOnJS(wrappedOnMoveForJSThread)({ move });
                   // Ideally I must call the resolve method
                   // inside the "wrappedOnMoveForJSThread" after
@@ -116,6 +145,7 @@ const Piece = React.memo(
           translateY,
           validateMove,
           wrappedOnMoveForJSThread,
+          refs,
         ]
       );
 
@@ -131,8 +161,8 @@ const Piece = React.memo(
         ref,
         () => {
           return {
-            moveTo: (to: Square) => {
-              return moveTo(square, to);
+            moveTo: (to: Square, force: boolean = false, isSilent: boolean = false) => {
+              return moveTo(square, to, force, isSilent);
             },
             enable: (active: boolean) => {
               pieceEnabled.value = active;
@@ -177,7 +207,7 @@ const Piece = React.memo(
           return;
         }
         if (!gestureEnabled.value) return;
-        scale.value = withTiming(1.2);
+        scale.value = withTiming(1);
         onStartTap(square);
       }, [
         gestureEnabled.value,
@@ -221,12 +251,12 @@ const Piece = React.memo(
       const style = useAnimatedStyle(() => {
         return {
           position: 'absolute',
-          opacity: withTiming(pieceEnabled.value ? 1 : 0),
+          opacity: withTiming(pieceEnabled.value ? 1 : 0, { duration: moveDuration }),
           zIndex: isGestureActive.value ? 100 : 10,
           transform: [
             { translateX: translateX.value },
             { translateY: translateY.value },
-            { scale: scale.value },
+            { scale: withTiming(pieceEnabled.value ? scale.value : 0, { duration: moveDuration }) },
           ],
         };
       });
@@ -243,9 +273,7 @@ const Piece = React.memo(
           height: size * 2,
           borderRadius: size,
           zIndex: 0,
-          backgroundColor: isGestureActive.value
-            ? 'rgba(0, 0, 0, 0.1)'
-            : 'transparent',
+          backgroundColor: 'transparent',
           transform: [
             { translateX: translation.x - size / 2 },
             { translateY: translation.y - size / 2 },
diff --git a/node_modules/react-native-chessboard/src/components/pieces.tsx b/node_modules/react-native-chessboard/src/components/pieces.tsx
index 5410d88..c2b05b1 100644
--- a/node_modules/react-native-chessboard/src/components/pieces.tsx
+++ b/node_modules/react-native-chessboard/src/components/pieces.tsx
@@ -18,15 +18,14 @@ const Pieces = React.memo(() => {
       {board.map((row, y) =>
         row.map((piece, x) => {
           if (piece !== null) {
-            const square = toPosition({
-              x: x * pieceSize,
-              y: y * pieceSize,
-            });
+            const col = String.fromCharCode(97 + x);
+            const row = `${8 - y}`;
+            const square = `${col}${row}` as Square;
 
             return (
               <Piece
                 ref={refs?.current?.[square]}
-                key={`${x}-${y}`}
+                key={`${x}-${y}-${piece.color}${piece.type}`}
                 id={`${piece.color}${piece.type}` as const}
                 startPosition={{ x, y }}
                 square={square}
diff --git a/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx b/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
index 2fc9565..f9472eb 100644
--- a/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-operations-context/index.tsx
@@ -144,6 +144,7 @@ const BoardOperationsContextProviderComponent = React.forwardRef<
       onChessboardMoveCallback,
       setBoard,
       turn,
+      pieceRefs,
     ]
   );
 
@@ -189,13 +190,22 @@ const BoardOperationsContextProviderComponent = React.forwardRef<
 
       const validSquares = (chess.moves({
         square,
-      }) ?? []) as Square[];
+      }) ?? []) as string[];
+
+      const turnColor = chess.turn();
 
       // eslint-disable-next-line no-shadow
-      selectableSquares.value = validSquares.map((square) => {
-        const splittedSquare = square.split('x');
+      selectableSquares.value = validSquares.map((move) => {
+        if (move.startsWith('O-O-O')) {
+          return (turnColor === 'w' ? 'O-O-O c1' : 'O-O-O c8') as Square;
+        }
+        if (move.startsWith('O-O')) {
+          return (turnColor === 'w' ? 'O-O g1' : 'O-O g8') as Square;
+        }
+
+        const splittedSquare = move.split('x');
         if (splittedSquare.length === 0) {
-          return square;
+          return move as Square;
         }
 
         return splittedSquare[splittedSquare.length - 1] as Square;
diff --git a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
index 481593d..7b54c4c 100644
--- a/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-promotion-context/dialog/index.tsx
@@ -20,6 +20,8 @@ const PromotionDialog: React.FC<Required<BoardPromotionContextState>> =
         style={[
           {
             width: boardSize / 3,
+            top: boardSize / 3,
+            left: boardSize / 3,
           },
           styles.container,
         ]}
diff --git a/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx b/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
index 3966cd9..6eee264 100644
--- a/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/board-refs-context/index.tsx
@@ -76,7 +76,7 @@ const BoardRefsContextProviderComponent = React.forwardRef<
     ref,
     () => ({
       move: ({ from, to }) => {
-        return pieceRefs?.current?.[from].current?.moveTo?.(to);
+        return pieceRefs?.current?.[from].current?.moveTo?.(to, true);
       },
       highlight: ({ square, color }) => {
         squareRefs.current?.[square].current.highlight({
diff --git a/node_modules/react-native-chessboard/src/context/props-context/index.tsx b/node_modules/react-native-chessboard/src/context/props-context/index.tsx
index 448aa9c..51198a1 100644
--- a/node_modules/react-native-chessboard/src/context/props-context/index.tsx
+++ b/node_modules/react-native-chessboard/src/context/props-context/index.tsx
@@ -62,13 +62,17 @@ type ChessboardProps = {
    * Useful if you want to customise the default durations used in the chessboard (in milliseconds).
    */
   durations?: ChessboardDurationsType;
+  /**
+   * Orientation of the board ('white' or 'black').
+   */
+  boardOrientation?: 'white' | 'black';
 };
 
 type ChessboardContextType = ChessboardProps &
   Required<
     Pick<
       ChessboardProps,
-      'gestureEnabled' | 'withLetters' | 'withNumbers' | 'boardSize'
+      'gestureEnabled' | 'withLetters' | 'withNumbers' | 'boardSize' | 'boardOrientation'
     >
   > & { pieceSize: number } & {
     colors: Required<ChessboardColorsType>;
@@ -80,6 +84,7 @@ const DEFAULT_BOARD_SIZE = Math.floor(SCREEN_WIDTH / 8) * 8;
 
 const defaultChessboardProps: ChessboardContextType = {
   gestureEnabled: true,
+  boardOrientation: 'white',
   colors: {
     black: '#62B1A8',
     white: '#D9FDF8',
diff --git a/node_modules/react-native-chessboard/src/notation.ts b/node_modules/react-native-chessboard/src/notation.ts
index 1484f29..c3ffca1 100644
--- a/node_modules/react-native-chessboard/src/notation.ts
+++ b/node_modules/react-native-chessboard/src/notation.ts
@@ -5,7 +5,7 @@ import { useChessboardProps } from './context/props-context/hooks';
 import type { Vector } from './types';
 
 const useReversePiecePosition = () => {
-  const { pieceSize } = useChessboardProps();
+  const { pieceSize, boardOrientation } = useChessboardProps();
 
   const toTranslation = useCallback(
     (to: Square) => {
@@ -16,26 +16,36 @@ const useReversePiecePosition = () => {
       if (!col || !row) {
         throw new Error('Invalid notation: ' + to);
       }
-      const indexes = {
-        x: col.charCodeAt(0) - 'a'.charCodeAt(0),
-        y: parseInt(row, 10) - 1,
-      };
+      const isFlipped = boardOrientation === 'black';
+      let indexX = col.charCodeAt(0) - 'a'.charCodeAt(0);
+      let indexY = parseInt(row, 10) - 1;
+      if (isFlipped) {
+        indexX = 7 - indexX;
+        indexY = 7 - indexY;
+      }
       return {
-        x: indexes.x * pieceSize,
-        y: 7 * pieceSize - indexes.y * pieceSize,
+        x: indexX * pieceSize,
+        y: 7 * pieceSize - indexY * pieceSize,
       };
     },
-    [pieceSize]
+    [pieceSize, boardOrientation]
   );
 
   const toPosition = useCallback(
     ({ x, y }: Vector) => {
       'worklet';
-      const col = String.fromCharCode(97 + Math.round(x / pieceSize));
-      const row = `${8 - Math.round(y / pieceSize)}`;
+      const isFlipped = boardOrientation === 'black';
+      let colIndex = Math.round(x / pieceSize);
+      let rowIndex = Math.round(y / pieceSize);
+      if (isFlipped) {
+        colIndex = 7 - colIndex;
+        rowIndex = 7 - rowIndex;
+      }
+      const col = String.fromCharCode(97 + colIndex);
+      const row = `${8 - rowIndex}`;
       return `${col}${row}` as Square;
     },
-    [pieceSize]
+    [pieceSize, boardOrientation]
   );
 
   return { toPosition, toTranslation };
